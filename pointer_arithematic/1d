#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    int num = 20;
    int *p = &num;
    //printf("%p\t", p);
    p = p + 1;
    //printf("%p\t", p);
    int arr[5] = { 1,2,3,4,5 };
    int* ptr = arr;
    printf("%d", *(arr+1));
   //printf("%d\t", *p);
    //printf("%d", sizeof(p));
    return 0;
}
